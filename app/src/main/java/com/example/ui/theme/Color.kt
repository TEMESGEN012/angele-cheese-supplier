package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Pristine Fresh Dairy Blue palette (Replacing the old unappealing green)
val DairyBluePrimary = Color(0xFF005691)     // Fresh royal milk blue
val DairySkyAccent = Color(0xFF0EA5E9)       // Ice sky blue accent
val SoftOrangeAccent = Color(0xFFFB923C)     // Beautiful warm, soft orange accent (Orange 400)

// Backwards-compatible aliases to ensure components compile flawlessly
val FarmGreenPrimary = DairyBluePrimary
val SageMutedAccent = DairySkyAccent

val ButterGoldSecondary = Color(0xFFF59E0B)   // Warm rich butter yellow (Amber 600)
val HoneyAmberTertiary = Color(0xFFFBBF24)    // Soft cream gold tone (Amber 400)

// Light theme background colors (Fresh, crisp, clean-room feel)
val CreamLightBackground = Color(0xFFFFFFFF)  // Pristine pure white background
val EggshellLightSurface = Color(0xFFF1F5F9)  // Soft slate-white for cards/surfaces (Slate 100)
val CoffeeDarkText = Color(0xFF0F172A)        // Soft high-contrast dark slate text (Slate 900)

// Dark theme background colors (Sophisticated dark mode)
val EarthDarkBackground = Color(0xFF0B0F19)   // Deep royal midnight Navy-black background
val CocoaDarkSurface = Color(0xFF1E293B)      // Dark slate card surface (Slate 800)
val MilkLightText = Color(0xFFF8FAFC)         // Crisp milk-white text (Slate 50)
